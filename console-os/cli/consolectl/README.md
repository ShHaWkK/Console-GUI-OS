# consolectl (phase 4, TODO)

Pas de CLI utilisateur factice. Le programme tests/ipc_client.cpp sert uniquement
à la recette D-Bus ; il n'est pas installé et n'est pas le futur consolectl.
Le contrat fonctionnel proposé figure dans docs/devkit.md.
